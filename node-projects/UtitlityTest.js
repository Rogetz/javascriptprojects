var utilityVar = require('util')
var objectToBeInspected = [20,30,50,60
,70]

// I have use the util module's inspect method. What it does is simply output the object passed to it as a parameter.

console.log(utilityVar.inspect(objectToBeInspected))